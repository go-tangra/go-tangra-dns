# Configuration for DNS service OpenAPI generation
version: v2

clean: false

managed:
  enabled: true

  disable:
    - module: buf.build/googleapis/googleapis
    - module: buf.build/bufbuild/protovalidate
    - module: buf.build/kratos/apis

  override:
    - file_option: go_package
      path: dns/service/v1
      value: github.com/go-tangra/go-tangra-dns/gen/go/dns/service/v1;dnspb

inputs:
  - directory: protos
    paths:
      - protos/dns/service/v1

plugins:
  # generate openapi v3 yaml doc
  - local: protoc-gen-openapi
    out: cmd/server/assets
    opt:
      - naming=json
      - depth=2
      - default_response=false
      - enum_type=string
      - output_mode=merged
      - fq_schema_naming=false
